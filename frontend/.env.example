# Leave blank for local dev — Vite's dev server proxies /api to localhost:5000 automatically.
# REQUIRED before deploying: set this to your live backend's URL + /api, e.g.
# VITE_API_URL=https://amara-api.onrender.com/api
VITE_API_URL=
